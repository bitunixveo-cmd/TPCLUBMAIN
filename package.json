{
  "name": "tpclub",
  "version": "1.0.0",
  "description": "Production-ready static landing page for TP Club.",
  "type": "commonjs",
  "scripts": {
    "dev": "npm run build && vite dist --host 0.0.0.0",
    "build": "node scripts/build-static.js && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
