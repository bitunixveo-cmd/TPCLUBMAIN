{
  "name": "tpclub",
  "version": "1.0.0",
  "description": "Production-ready static landing page for TP Club.",
  "type": "commonjs",
  "scripts": {
    "dev": "npm run build && vite preview --host 0.0.0.0",
    "build": "node scripts/build-static.js && vite build",
    "build:with-ads": "npm run sync:ads && npm run build",
    "preview": "vite preview --host 0.0.0.0",
    "refresh:ads": "npm run sync:ads",
    "sync:ads": "node scripts/sync-ads-data.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "framer-motion": "^12.40.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "react-router-dom": "^7.15.1",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
